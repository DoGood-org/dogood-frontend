import { ProfileMapSection } from '@/components/main/map/profileMap/ProfileMapSection';
import React from 'react';

const MapPage: React.FC = () => {
  return <ProfileMapSection />;
};

export default MapPage;
